"""Engram-shipped scripts (hooks, helpers).

These scripts are stdlib-only and intended to be invoked directly via
their resolved file path (not via `python -m`), so they do not pay the
cost of importing the rest of the engram package.
"""
